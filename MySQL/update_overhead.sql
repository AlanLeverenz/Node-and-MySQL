USE bamazon;

UPDATE departments
SET over_head_costs = 2000
WHERE department_id = 1;

UPDATE departments
SET over_head_costs = 3000
WHERE department_id = 2;

UPDATE departments
SET over_head_costs = 3000
WHERE department_id = 3;

UPDATE departments
SET over_head_costs = 5000
WHERE department_id = 4;

UPDATE departments
SET over_head_costs = 4000
WHERE department_id = 5;

UPDATE departments
SET over_head_costs = 5000
WHERE department_id = 6

